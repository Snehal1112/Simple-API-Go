package v1




import (
			
	// tasks "simple-api/controllers/v1/task" // Un-comment when the handler is ready

	"gopkg.in/gin-gonic/gin.v1"	

)


func SetTaskRoutes(router *gin.RouterGroup) {
	
	/*
	*
	* Un-Comment when the handler is ready
	*
	**/

 /*
	router.GET("/tasks", tasks.GetAllTask)
	router.GET("/tasks/:task_id", tasks.GetOneTask)
	router.POST("/tasks", tasks.CreateTask)
	router.PUT("/tasks/:task_id", tasks.UpdateTask)
	router.DELETE("/tasks/:task_id", tasks.DeleteTask)*/
}
