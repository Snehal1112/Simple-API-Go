package v1 


import (
	"gopkg.in/gin-gonic/gin.v1"
	// tasks "simple-api/controllers/v1/tasks"
	// users "simple-api/controllers/v1/users"
)

func SetUserRoutes(router *gin.RouterGroup) {
	
	/*
	*
	*Un-Comment when the function is finished
	*
	*
	router.GET("/users", users.GetAllUser)
	router.GET("/users/:user_id", users.GetOneUser)
	router.GET("/users/:user_id/tasks", tasks.GetAllTaskByUser)
	
	router.POST("/users", users.CreateUser)
	router.PUT("/users/:user_id", users.UpdateUser)
	router.DELETE("/users/:user_id", users.DeleteUser)
*/
}

